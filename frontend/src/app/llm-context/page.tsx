export default function LlmContextPage() {
  return (
    <section className="section-space">
      <div className="site-container">
        <h1 className="text-4xl font-bold text-foreground">LLM Context</h1>
        <p className="mt-4 text-text-muted">
          This hidden page will expose structured business context for AI search
          optimization in a later phase.
        </p>
      </div>
    </section>
  );
}
